apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mage-volume
  namespace: mageai
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 8Gi